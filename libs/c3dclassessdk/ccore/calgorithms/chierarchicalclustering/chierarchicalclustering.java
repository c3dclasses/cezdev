//--------------------------------------------------
// file: chierarchicalclustering
// desc: 
//--------------------------------------------------
package c3dclasses;

//---------------------------------------------------------------------
// name: chierarchicalclustering
// desc: 
//---------------------------------------------------------------------
public class chierarchicalclustering {
	/*
	public static CArray _(CArray dataset, int k) {
			return null;
	} // end _()
	
	public static CArray compute_pairwise_distance(CArray dataset) {
		int l = dataset.length();
		for(int i=0; i<l; i++) {
			for(int j=i+1; j<l; j++) {
				float dist = cmath.euclidiean_distance(dataset._carray(i),dataset._carray(j));
			} // end for
		} // end for
		return result;
	} // end compute_pairwise_distance()
	*/
} // end chierarchicalclustering